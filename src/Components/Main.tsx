import React from "react";
import { Typography, Box } from "@mui/material";

const Main = () => {
  return (
    <Box>
      <Typography>Hello 👋 World 🌍</Typography>
      <Typography variant="h1">Roboto</Typography>
      <Typography variant="h2">Roboto</Typography>
      <Typography variant="h3">Roboto</Typography>
      <Typography variant="h4">Roboto</Typography>
      <Typography variant="h5">Roboto</Typography>
      <Typography variant="h6">Roboto</Typography>
      <Typography variant="body1">Roboto</Typography>
      <Typography variant="body2">Roboto</Typography>
    </Box>
  );
};

export default Main;
